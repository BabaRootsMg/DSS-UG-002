<!DOCTYPE html>
<html>
<head>
  <title>Verify Your Account</title>
  <meta charset="utf-8">
  <link rel="icon" type="image/x-icon" href="/imgs/foodie.ico">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <a href="/"><img id="logo" src="/imgs/logo.png" alt="Logo"></a>
    <h1>Account Verification</h1>
  </header>

  <main>
    <p>Enter the 6-digit verification code sent to your email.</p>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form action="/verify" method="POST" id="login_form">
      <input type="hidden" name="email" value="<%= email %>" />
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

      <label for="code">Verification Code:</label>
      <input name="code" id="code" placeholder="6-digit code" required />

      <button id="login_btn" type="submit">Verify &amp; Login</button>
    </form>

  </main>
</body>
</html>
